apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "<CHARTNAME>.webAppName" . }}
  labels:
    {{- include "<CHARTNAME>.webLabels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "<CHARTNAME>.webSelectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "<CHARTNAME>.webSelectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "<CHARTNAME>.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ include "<CHARTNAME>.appName" . }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: {{ required "missing required value: .Values.image.repository" .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}
          imagePullPolicy: {{ include "<CHARTNAME>.imagePullPolicy" . }}
          args:
            - sh
            - -c
            - {{ include "<CHARTNAME>.webCommand" . }}
          ports:
            - name: http
              containerPort: {{ .Values.service.containerPort }}
              protocol: {{ .Values.service.protocol }}
          livenessProbe:
            {{- include "<CHARTNAME>.livenessProbe" . | nindent 12 }}
          readinessProbe:
            {{- include "<CHARTNAME>.readinessProbe" . | nindent 12 }}
          resources:
            {{- include "<CHARTNAME>.resources" . | nindent 12 }}
          envFrom:
            - configMapRef:
                name: {{ include "<CHARTNAME>.fullname" . }}-config
            - secretRef:
                name: {{ include "<CHARTNAME>.fullname" . }}-secret
          volumeMounts:
          - name: {{ include "<CHARTNAME>.appName" . }}
            mountPath: {{ required "missing required value: .Values.config.fileUploadPath" .Values.config.fileUploadPath }}
      volumes:
      - name: {{ include "<CHARTNAME>.appName" . }}
        persistentVolumeClaim:
          claimName: {{ include "<CHARTNAME>.claimName" . }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      affinity:
        {{- include "<CHARTNAME>.webPodAntiAffinity" . | nindent 8 }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
