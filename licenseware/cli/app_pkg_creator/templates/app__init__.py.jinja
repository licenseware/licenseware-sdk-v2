# Uncomment bellow 2 lines to run app with: `python3 main.py` url at: localhost:5000/api/docs
# from licenseware.utils.miscellaneous import set_environment_variables (set MONGOLOCAL to true to use embeded mongo version)
# set_environment_variables(envs={"ENVIRONMENT": "desktop", "MONGOLOCAL": "false"})
from licenseware.common.constants import flags
from licenseware.app_builder import AppBuilder


App = AppBuilder(
    name='Service Name',
    description='Service description',
    flags=[flags.BETA]
)




